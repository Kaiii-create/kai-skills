# Local Edit Workflow

A user selection/point/description defines the target. Outside target is locked.

Examples: remove one pimple, brighten right eye slightly, remove a person behind, repair dress wrinkle, fill a small hair gap, remove a wire.

Use the smallest feasible mask and operation. Feather only enough for seamless blending. Compare before/after outside the mask; unintended changes are QC failure.
